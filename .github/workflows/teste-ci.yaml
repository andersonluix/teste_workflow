name: Workflow

on:
  push:
    paths:
      - 'IaC/**'
  push:
    paths:
      - 'frontend/**'
  push:
    paths:
      - '!IaC/**'
      - '!frontend/**'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Print Build
        run: echo "Build"

  terraform:
    runs-on: ubuntu-latest

    steps:
      - name: Print Terraform
        run: echo "Terraform"

  manual:
    runs-on: ubuntu-latest
    if: github.event_name == 'manual'

    steps:
      - name: Print Manual
        run: echo "Manual"
